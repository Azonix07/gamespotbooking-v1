# Production environment variables
CI=false
GENERATE_SOURCEMAP=false

# Backend API URL - Empty means same-origin (frontend and backend on same domain)
# This fixes mobile cookie issues by avoiding cross-origin requests
REACT_APP_API_URL=
