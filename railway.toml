[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && npm install && npm run build"

[deploy]
startCommand = "cd frontend && node test-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
