# Database Configuration
DB_HOST=localhost
DB_NAME=gamespot_booking
DB_USER=root
DB_PASS=newpassword

# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_ENV=development

# ============ EMAIL CONFIGURATION ============
# Priority: Brevo → Resend → SMTP → disabled

# Option A (RECOMMENDED for Railway): Brevo HTTP API
#   ✅ No custom domain needed — just verify your Gmail in Brevo dashboard
#   ✅ Works on Railway (uses HTTPS, not blocked SMTP ports)
#   ✅ Free: 300 emails/day
#   Steps:
#     1. Sign up free at https://brevo.com
#     2. Verify your sender email in Settings → Senders & IPs
#     3. Get your API key from Settings → SMTP & API → API Keys
#     4. Set BREVO_API_KEY and SMTP_EMAIL below
BREVO_API_KEY=xkeysib-your-api-key-here
SMTP_EMAIL=youremail@gmail.com

# Option B: Resend HTTP API
#   ⚠️ Requires a verified CUSTOM DOMAIN (cannot use gmail.com)
#   ⚠️ onboarding@resend.dev can ONLY send to your own Resend account email
#   1. Sign up at https://resend.com (100 emails/day free)
#   2. Verify a domain at resend.com/domains
#   3. Set RESEND_API_KEY and RESEND_FROM_EMAIL below
RESEND_API_KEY=re_your_api_key_here
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# Option C: Gmail SMTP (does NOT work on Railway — ports blocked)
#   Only use this for local development or hosts that allow SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SMTP_EMAIL=youremail@gmail.com  (already set above for Brevo)
SMTP_PASSWORD=your-16-char-app-password

# Frontend URL (for email links)
FRONTEND_URL=https://gamespotweb-production.up.railway.app

# AI Configuration - Google Gemini (FREE)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests/minute
GEMINI_API_KEY=your_gemini_api_key_here
