# Database Configuration
DB_HOST=localhost
DB_NAME=gamespot_booking
DB_USER=root
DB_PASS=newpassword

# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_ENV=development

# ============ EMAIL CONFIGURATION ============
# Option A (RECOMMENDED for Railway): Resend HTTP API
#   1. Sign up free at https://resend.com (100 emails/day free)
#   2. Get your API key from the dashboard
#   3. Set RESEND_API_KEY below
#   4. Use onboarding@resend.dev as sender, or verify your own domain
RESEND_API_KEY=re_your_api_key_here

# Option B: Gmail SMTP (does NOT work on Railway — ports blocked)
#   Only use this for local development or hosts that allow SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=youremail@gmail.com
SMTP_PASSWORD=your-16-char-app-password

# Frontend URL (for email links)
FRONTEND_URL=https://gamespotweb-production.up.railway.app

# AI Configuration - Google Gemini (FREE)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests/minute
GEMINI_API_KEY=your_gemini_api_key_here
