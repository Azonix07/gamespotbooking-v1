# Database Configuration
DB_HOST=localhost
DB_NAME=gamespot_booking
DB_USER=root
DB_PASS=newpassword

# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_ENV=development

# ============ EMAIL CONFIGURATION ============
# Priority: Resend → Brevo → SMTP → disabled

# Option A (RECOMMENDED): Resend HTTP API + Verified Domain
#   ✅ Works on Railway (uses HTTPS, not blocked SMTP ports)
#   ✅ Free: 100 emails/day (3000/month)
#   Steps:
#     1. Sign up at https://resend.com
#     2. Verify your domain at resend.com/domains (e.g. gamespotkdlr.com)
#     3. Set DNS records (SPF + DKIM) as shown in Resend dashboard
#     4. Set RESEND_API_KEY below
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@gamespotkdlr.com

# Option B: Brevo HTTP API (no custom domain needed)
#   ✅ Free: 300 emails/day
#   Just verify your Gmail address in Brevo dashboard
BREVO_API_KEY=xkeysib-your-api-key-here
SMTP_EMAIL=youremail@gmail.com

# Option C: Gmail SMTP (does NOT work on Railway — ports blocked)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SMTP_EMAIL=youremail@gmail.com
SMTP_PASSWORD=your-16-char-app-password

# Frontend URL (for email links)
FRONTEND_URL=https://gamespotweb-production.up.railway.app

# AI Configuration - Google Gemini (FREE)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests/minute
GEMINI_API_KEY=your_gemini_api_key_here
